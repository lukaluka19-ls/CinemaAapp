const movies = [
    {
      title: "DareDevil",
      image: slika11,
    },
    {
      title: "Michael",
      image: slika10,
    },
    {
      title: "Sparta",
      image: slika9,
    },
    {
      title: "Punisher",
      image: slika8,
    },
    // {
    //   title: "John Wick",
    //   image: slika6,
    // },
    {
      title: "San Andreas",
      image: slika3,
    },
    {
      title: "Michael",
      image: slika2,
    },
    {
      title: "Joker",
      image: slika4,
    },
    {
      title: "John Wick",
      image: slika5,
    },
  ];

<div className="relative min-h-screen w-full overflow-hidden">
        {images.map((image, index) => (
          <div
            key={index}
            className={`absolute bg-white inset-0 bg-cover bg-center transition-opacity duration-1000 ${
              index === currentImage ? "opacity-100" : "opacity-0"
            }`}
            style={
              {
                // backgroundImage: `url('${image}')`,
              }
            }
          />
        ))}

        <div className="absolute inset-0 bg-black"></div>
        <div className="relative z-10 p-10">
          <h1 className="text-white text-5xl font-bold mb-10">
            Popular Movies
          </h1>

          <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-8">
            {movies.map((movie, index) => (
              <div
                key={index}
                className="bg-zinc-900/80 backdrop-blur-md rounded-3xl overflow-hidden shadow-2xl cursor-pointer"
                //hover:scale-105 transition duration-300 cursor-pointer
              >
                <img
                  src={movie.image}
                  alt={movie.title}
                  className="w-full h-[420px] object-cover"
                />

                <div className="p-5 flex justify-center">
                  <h2 className="text-white text-2xl font-semibold">
                    {movie.title}
                  </h2>

                  <p className="text-zinc-400 mt-2 text-sm font-sans ">
                    {/* • Cinema App presents */}
                  </p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>

      